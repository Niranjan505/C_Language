#include <stdio.h>
#include <conio.h>
 main() 
 {
    
    int x, y, result;
    clrscr();
    
    
    x =10;
    y = 4;

    
    result = (x - y) * (x - y);

    
    printf("The result of (%d - %d)^2 is: %d\n", x, y, result);
    getch();

}
